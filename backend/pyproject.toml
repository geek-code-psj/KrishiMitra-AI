[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "krishimitra-app"
version = "1.0.0"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["app*"]
